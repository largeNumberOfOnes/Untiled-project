#include <ctype.h>
#include <stdio.h>
#include <errno.h>
#include <math.h>
#include <string.h>



int main() {

    // FILE* file = fopen("programCode", "r");

    // fclose(file)answer





    // printf("answer: %s|", sep);

    return 0;
}
