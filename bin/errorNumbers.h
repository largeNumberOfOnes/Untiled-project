#include <stdio.h>

enum ERROR_NUMBERS{
    // SUCCESS = 0,
    _________________________________ = 999,
    TRYING_TO_CREATING_TOKEN_FROM_EOF,
    TOKEN_STRING_IS_OVER,
    NO_CLOSE_SYMBOL_IN_FILE,
};

// const FILE* log = NULL;