test: test.c
	gcc test.c -o test



# docs --------------------------------------------------------
scanner_graph: scanner_graph.graph
	dot -Tsvg scanner_graph.graph -o scanner_graph.svg



# simpleLib ---------------------------------------------------

simpleLib: --
	--

a=$(find | grep '.c$')
run:
	echo '$(find | grep '.c$')'
# gcc $(ls -R | grep '.c$') -o out


